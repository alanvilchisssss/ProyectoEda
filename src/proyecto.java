import java.util.Scanner;
public class proyecto {
    public static void main(String[] args) {
        Scanner scanner=new Scanner(System.in);
        utilerias.Utilerias.menu(scanner);    
        scanner.close();
    }
}
